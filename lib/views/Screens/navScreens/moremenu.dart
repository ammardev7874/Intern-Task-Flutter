import 'package:flutter/material.dart';
import 'package:interntask/widgets/appbar.dart'; // Import the custom AppBar

class MoreScreen extends StatelessWidget {
  const MoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: const Color(0xFFF2F2F2),)
    );
  }
}





